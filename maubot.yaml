maubot: 0.6.0
id: xyz.maubot.gitlab-testing
version: 0.2.2
license: AGPL-3.0-or-later
modules:
- gitlab_matrix
main_class: GitlabBot

extra_files:
- base-config.yaml
- templates/macros.html
- templates/messages/*.html
- templates/mixins/*.html

soft_dependencies:
# Not needed if only used as webhook (TODO)
- python-gitlab

webapp: true
database: true
config: true
